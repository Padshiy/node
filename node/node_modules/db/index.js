var pharses;

exports.connect = function () {
    pharses = require('./ru');
}

exports.getPhrase = function (name) {
    if (!pharses[name]) {
        throw new Error("Нет такой фразы: " + name);
    } else {
        return pharses[name];
    }
}